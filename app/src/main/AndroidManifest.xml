<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="creativej.gymtrainer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fitness"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Splesh_Activity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".One_Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BasicEx" />
        <activity android:name=".TipOfThe" />
        <activity
            android:name=".FinalPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Workouts"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".DietPlanActivity" />
        <activity android:name=".MoreActivity"
            android:parentActivityName=".MainActivity"></activity>
    </application>

</manifest>